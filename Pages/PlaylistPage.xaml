<Page
    x:Class="FluentNetease.Pages.PlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentNetease.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="using:FluentNetease.Classes"
    xmlns:controls="using:FluentNetease.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlTransparentBrush}">

    <controls:MusicList
        ItemsSource="{x:Bind Path=Songs}">
        <controls:MusicList.Header>
            <Grid
                Margin="0,8,16,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="270"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border
                    Height="240"
                    Width="240"
                    Grid.Column="0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    CornerRadius="8">
                    <Image
                        x:Name="CoverPicture"
                        Height="240"
                        Width="240"
                        Stretch="Fill"
                        Source="{x:Bind Path=Playlist.CoverPictureUrl,Mode=OneWay}"/>
                </Border>
                <Grid
                    Height="240"
                    Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="40"/>
                    </Grid.RowDefinitions>
                    <TextBlock
                        x:Name="TitleText"
                        Grid.Row="0"
                        VerticalAlignment="Center"
                        TextWrapping="Wrap"
                        FontSize="30"
                        FontWeight="Normal"
                        Text="{x:Bind Path=Playlist.Name,Mode=OneWay}"/>
                    <ScrollViewer
                        Grid.Row="1"
                        VerticalScrollMode="Enabled">
                        <TextBlock
                            x:Name="DescriptionText"
                            Grid.Row="1"
                            Margin="2,8,16,0"
                            TextWrapping="Wrap"
                            Foreground="{ThemeResource DescriptionTextBrush}"
                            Text="{x:Bind Path=Playlist.Description,Mode=OneWay}"/>
                    </ScrollViewer>
                    <Grid
                        Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Button
                            x:Name="PlayButton"
                            Height="30"
                            VerticalAlignment="Bottom"
                            Click="PlayButton_Click">
                            <SymbolIcon
                                Symbol="Play"/>
                        </Button>
                    </Grid>
                </Grid>
            </Grid>
        </controls:MusicList.Header>
    </controls:MusicList>
</Page>
