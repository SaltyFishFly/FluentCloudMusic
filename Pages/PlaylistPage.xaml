<Page
    x:Class="FluentCloudMusic.Pages.PlaylistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FluentCloudMusic.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="using:FluentCloudMusic.Services"
    xmlns:controls="using:FluentCloudMusic.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlTransparentBrush}">

    <controls:MusicListView
        x:Name="MusicList"
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        ItemsSource="{x:Bind Path=Songs}">
        <controls:MusicListView.Header>
            <controls:ResourceDetailPresenter
                ImageSource="{x:Bind Path=PlaylistInfo.CoverPictureUrl,Mode=OneWay}"
                Title="{x:Bind Path=PlaylistInfo.Name,Mode=OneWay}"
                Description="{x:Bind Path=PlaylistInfo.Description,Mode=OneWay}"/>
        </controls:MusicListView.Header>
    </controls:MusicListView>
</Page>
